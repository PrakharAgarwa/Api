language: python
python:
 - "3.5"
 before_install:
  - python -m pip install --upgrade pip
install:
  - pip install --user -r requirements.txt --use-feature=2020-resolver

# command to run tests
script:
 - python -m unittest TestGit
